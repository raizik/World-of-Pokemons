CREATE TABLE POKEMON (ID INT PRIMARY KEY,
                    NAME VARCHAR(25),
                    TYPE VARCHAR(25),
                    NAMET VARCHAR(25),
                    INSERTIME INT);

CREATE TABLE TRAINERS (NAME VARCHAR(25) PRIMARY KEY,
                        LEVEL INT,
                        BAGSIZE INT);

